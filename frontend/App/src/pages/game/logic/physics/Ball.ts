export class Ball {
    constructor(x: number, y: number, radius: number, color: string) {
        this.x = x;
        this.y = y;
        this.radius = radius;
        this.color = color;
    }

    x: number;
    y: number;
    radius: number;
    color: string;
}
